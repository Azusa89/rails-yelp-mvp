<div class="page">
  <h1>Restaurants</h1>
  <div class="grid">
    <%@restaurants.each do |i| %>
    <div class = grid_item>
      <h5><%= link_to i.name, restaurant_path(i) %></h5></div>
    <%end %>
  </div>
  <p><%= link_to "New restaurant", new_restaurant_path(@restaurant) %></p>
</div>
